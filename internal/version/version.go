// Package version exposes the CrawlWall release version.
package version

// Version is the CrawlWall release version. The config schema version
// (crawlwall.io/v1) is separate and lives in the config package.
const Version = "v1.0.0-alpha"
